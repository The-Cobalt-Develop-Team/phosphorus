#include <Vector.hpp>
#include <bits/stdc++.h>

using namespace std;

Vector a(3, 0), b(4, -90);

int main()
{
    cout << sin(-1.6667) << endl;
    cout << (a + b).returnData() << ' ' << (a + b).returnDegree() << endl;
}